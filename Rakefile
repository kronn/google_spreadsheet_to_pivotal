desc "setup briding app on heroku"
task :setup do
  require 'heroku'
  heroku = Heroku::Auth.client

  # try to get the app to fail fast
  # detected_app = app

  # # define the vars
  # vars = {
  #   "SPREADSHEET_USERNAME"  => "someone@googlemail.com",
  #   "SPREADSHEET_PASSWORD"  => "password",
  #   "SPREADSHEET_KEY"       => "pz7XtlQC-PYx-jrVMJErTcg",
  #   "SPREADSHEET_MAPPING"   => "id-slug-name--estimate",
  #   "SPREADSHEET_REQUESTOR" => "Product Owner",
  #   "HTTP_BASIC_USERNAME"   => "pivotal",
  #   "HTTP_BASIC_PASSWORD"   => "password"
  # }

  # # add the config
  # heroku.add_config_vars(detected_app, vars)
end
